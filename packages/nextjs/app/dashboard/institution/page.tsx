import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
};

const Dashboard = () => {
  return <div>Institution Dashboard</div>;
};

export default Dashboard;
